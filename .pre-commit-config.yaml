# See https://pre-commit.com for more information
# See https://pre-commit.com/hooks.html for more hooks
exclude: ^\.github/aw/
repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
  - repo: https://github.com/DavidAnson/markdownlint-cli2
    rev: v0.17.2
    hooks:
      - id: markdownlint-cli2
        name: markdownlint-cli2
        description: "Markdown linter"
        entry: markdownlint-cli2
        language: node
        files: \.md$
        require_serial: true
        args: ["--config", ".markdownlint-cli2.jsonc", "--fix"]
  - repo: https://github.com/lycheeverse/lychee
    rev: v0.15.1
    hooks:
      - id: lychee
        name: lychee-link-check
        description: "Check for broken links in markdown files"
        args:
          - "--no-progress"
          - "--accept"
          - "200,429"
          - "--exclude"
          - "shields.io"
          - "--exclude"
          - "github.com/.*/badge"
          - "--exclude"
          - "gist.github.com"
        files: \.md$
        stages: [pre-push]
  - repo: local
    hooks:
      - id: check-markdown-links
        name: check-markdown-links
        description: "Check for broken file links in markdown files"
        entry: python3 scripts/check-markdown-links.py
        language: system
        files: \.md$
        pass_filenames: false
      - id: cclint
        name: cclint
        description: "Validate Claude Code instruction files"
        entry: bash -c 'command -v cclint >/dev/null 2>&1 && cclint || npx @carlrannaberg/cclint'
        language: system
        files: ^\.claude/
        pass_filenames: false
        stages: [pre-push]
      - id: validate-permissions
        name: validate-permissions
        description: "Validate permission files for cclint compatibility"
        entry: bash scripts/validate-permissions.sh
        language: system
        files: ^agentsmd/permissions/(?!valid-tools\.json).*\.json$
        pass_filenames: false
        stages: [pre-commit]
      - id: no-runner-git-commit
        name: no-runner-git-commit
        description: "Block raw git commit/push in workflow YAML (Layer 5 of signed-commits initiative)"
        entry: bash scripts/check-no-runner-git-commit.sh
        language: system
        files: '\.github/(workflows/.*\.ya?ml|actions/.+/action\.ya?ml)$'
        stages: [pre-commit]
